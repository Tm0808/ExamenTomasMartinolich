


const ErrorMessage = () => {
return (
    <div style={styles.error}>
    <p>Error: Por favor, verifica los datos ingresados.</p>
    </div>
);
};

const styles = {
error: {
    color: 'red',
    marginTop: '16px',
    textAlign: 'center',
}
};

export default ErrorMessage;












